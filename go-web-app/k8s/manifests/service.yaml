apiVersion: v1
kind: Service
metadata:
  name: go-web-app
  labels:
    go-web-app
spec:
  selector:
    app: web-go-app
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
